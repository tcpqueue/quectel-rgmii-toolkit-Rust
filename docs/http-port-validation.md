# HTTP 自定义端口验证（v0.2.3）

2026-09-06，RM520N-EU、armv7l、Linux 5.4.210-perf。

| 场景 | 结果 |
| --- | --- |
| 旧版 0.2.0 升级，指定 HTTP 8080 | 安装成功，端口文件为 8080，systemd active/enabled |
| 模块本机检查 | 首页、登录页和语言脚本检查通过 |
| 电脑 ADB 通道 | 自动读取 8080，HTTP 页面检查通过 |
| 局域网访问 | `http://192.168.225.1:8080/login.html` 返回 HTTP 200，内容标识正确 |
| 再次升级，不指定端口 | 保留 8080，端口文件 mtime 不变 |
| 应用服务重启 | 继续监听 8080，配置未重写，网页检查通过 |
| 移动网络限制 | 设备已有 rmnet_data0 的 TCP 80 DROP，8080 同步生成 DROP，位于 ACCEPT 之前；服务重启后保持 |
| 测试后恢复 | 恢复原端口 80，移除本次测试的 8080 防火墙规则及 ADB 转发，局域网页面正常 |
| 设备状态 | 全过程 boot_id 不变，安装结束及恢复后根目录为只读 |

没有执行短信发送、删除或 AT 功能实验。本次验证应用服务重启，未做模块断电重启测试。

自动检查：47 项 Rust 测试、26 项 Linux 安装检查、27 项 Windows 安装检查、24 项 GUI 流程。包括端口范围、配置损坏、升级保留、已占用端口拒绝、配置不重复写入、启动参数、诊断转发及移动网络防火墙限制。最终 EXE 另通过资源解包校验与启动自检，离线 ZIP 完整性通过。
